{
	"$schema": "http://json-schema.org/schema",
	"cli": "nx",
	"title": "Grammar executor",
	"description": "",
	"type": "object",
	"properties": {
		"outputPath": {
			"type": "string",
			"description": "Destination folder for the build output."
		},
		"install": {
			"type": "boolean",
			"description": "Install the local `.vsix` file after building. Implies `package=true`."
		},
		"publish": {
			"type": "boolean",
			"description": "Publish the local `.vsix` file after building. Implies `package=true`."
		}
	},
	"required": [
		"outputPath"
	]
}
